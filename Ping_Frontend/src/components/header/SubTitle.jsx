import React from "react";

import "./SubTitle.css";

export const SubTitle = () => (
  <h3 className="sub-title">
    Global directory of areas where the demand of help of organizations is in real need
  </h3>
);
